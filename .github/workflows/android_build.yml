name: Android Build


on:
  push:
    branches:
      - 'master'


jobs:
  build:

    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up ruby env
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.2.0-1 # omit if .ruby-version file exists in project, or replace with your team’s supported ruby version
          bundler-cache: true
      - run: fastlane build_dev